# Copy to ".env" for local dev (.env is gitignored — never commit real secrets).
# These two values are browser-safe (anon/publishable). They are the ONLY keys
# the frontend uses. Do NOT put the service-role key here.
VITE_SUPABASE_URL=https://gftkuwqfvvbomzkamwep.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_RCeMPeM5fB_n0wUo62d-3A_5qO57iRQ

# ── Anti-abuse gateway (ANTI-ABUSE-v2.8) — OFF by default ────────────────────
# Turn on ONLY after the Edge Functions are deployed + verified (see
# docs/anti-abuse-gateway-runbook.md). Both values below are PUBLIC and browser-safe.
VITE_TURNSTILE_ENABLED=false
VITE_TURNSTILE_SITE_KEY=
# NOTE: the Turnstile SECRET key is NOT set here. It is a server-side secret set on
# the Supabase Edge Functions via `supabase secrets set TURNSTILE_SECRET_KEY=...`.
# Never put the secret key in frontend env, .env, Git, logs, or screenshots.
