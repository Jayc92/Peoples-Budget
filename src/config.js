// App-level configuration. Update SHARE_URL to the real domain before launch;
// do not change the live deployment URL here without intent.
export const SHARE_URL = "thepeoplesbudget.us";

// Below this participant count, community results are shown as preliminary.
export const LOW_N_THRESHOLD = 25;
